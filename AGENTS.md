# Repository Instructions

- Build with TypeScript and React Native architecture-first patterns.
- Prefer feature modules under `src/modules` and shared layers under `src/core` and `src/shared`.
- Keep static learning content outside components (JSON or service driven).
- Add documentation for setup and architecture in `README.md`.
